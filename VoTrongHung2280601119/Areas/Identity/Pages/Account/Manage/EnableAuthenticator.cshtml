@page
@model EnableAuthenticatorModel
@{
    ViewData["Title"] = "Cấu hình ứng dụng xác thực";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />

<h3>@ViewData["Title"]</h3>

<div>
    <p>Để sử dụng ứng dụng xác thực hai bước, vui lòng thực hiện các bước sau:</p>
    <ol class="list">
        <li>
            <p>
                Tải xuống một ứng dụng xác thực như Microsoft Authenticator cho
                <a href="https://go.microsoft.com/fwlink/?Linkid=825072">Android</a> hoặc
                <a href="https://go.microsoft.com/fwlink/?Linkid=825073">iOS</a>,
                hoặc Google Authenticator cho
                <a href="https://play.google.com/store/apps/details?id=com.google.android.apps.authenticator2&amp;hl=vi">Android</a> và
                <a href="https://apps.apple.com/vn/app/google-authenticator/id388497605">iOS</a>.
            </p>
        </li>
        <li>
            <p>Quét mã QR hoặc nhập khóa sau <kbd>@Model.SharedKey</kbd> vào ứng dụng xác thực. Không phân biệt khoảng trắng và chữ hoa/thường.</p>
            <div class="alert alert-info">
                Hướng dẫn <a href="https://go.microsoft.com/fwlink/?Linkid=852423">bật tính năng tạo mã QR</a>.
            </div>
            <div id="qrCode"></div>
            <div id="qrCodeData" data-url="@Model.AuthenticatorUri"></div>
        </li>
        <li>
            <p>
                Sau khi quét mã QR hoặc nhập khóa, ứng dụng xác thực sẽ hiển thị một mã xác thực duy nhất. Hãy nhập mã đó vào ô bên dưới để xác nhận.
            </p>
            <div class="row">
                <div class="col-md-6">
                    <form id="send-code" method="post">
                        <div class="form-floating mb-3">
                            <input asp-for="Input.Code" class="form-control" autocomplete="off" placeholder="Nhập mã xác thực" />
                            <label asp-for="Input.Code" class="control-label form-label">Mã xác thực</label>
                            <span asp-validation-for="Input.Code" class="text-danger"></span>
                        </div>
                        <button type="submit" class="w-100 btn btn-lg btn-primary">Xác nhận</button>
                        <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
                    </form>
                </div>
            </div>
        </li>
    </ol>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
